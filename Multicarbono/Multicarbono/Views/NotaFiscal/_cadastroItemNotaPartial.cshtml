@model IEnumerable<Multicarbono.Models.ItemNota.ItemNota>
@*@{
        List<Multicarbono.Models.ItemPedido.ItemPedido> itemPedido = Newtonsoft.Json.JsonConvert.DeserializeObject< List<Multicarbono.Models.ItemPedido.ItemPedido>>(TempData["Pedido"].ToString());
        List<Multicarbono.Models.ItemNota.ItemNota> itemNF = Newtonsoft.Json.JsonConvert.DeserializeObject<List<Multicarbono.Models.ItemNota.ItemNota>>(TempData["ItensNota"].ToString());

    }*@


<table class="table table-striped table-hover" style="margin-left:1%; margin-right:1%">
    <thead>
        <tr>
            @*<th>
                    @Html.DisplayNameFor(itemNota => itemNota.IdItemNF)
                </th>
                <th>
                    @Html.DisplayNameFor(itemNota => itemNota.IdNF)
                </th>*@
            <!--<th>-->
            @*@Html.DisplayNameFor(itemNota => itemNota.IdItemPedido)*@
            <!--</th>-->
            <th>
                @Html.DisplayNameFor(itemNota => itemNota.CodProduto)
            </th>
            <th>
                @Html.DisplayNameFor(itemNota => itemNota.QtdePedido)
            </th>
            <th>
                @Html.DisplayNameFor(itemNota => itemNota.SubtotalItemNota)
            </th>
            <th>
                @Html.DisplayNameFor(itemNota => itemNota.QtdePesada)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <form asp-controller="ItemNota" asp-action="CadastroItemNota" style="margin-left:1%; margin-right:1%">
                    @*<td readonly="true">
                            <input asp-for="@item.IdNF" class="form-control" value="@item.IdNF" style="visibility:hidden" />
                        </td>
                        <td readonly="true">
                            <input asp-for="@item.IdItemPedido" class="form-control" value="@item.IdItemPedido" style="visibility:hidden" />
                        </td>*@
                    <td>
                        <input asp-for="@item.CodProduto" class="form-control" value="@item.CodProduto" readonly="readonly" />
                    </td>
                    <td>
                        <input asp-for="@item.QtdePedido" class="form-control" value="@item.QtdePedido" readonly="readonly" />
                    </td>
                    <td readonly="true">
                        <input asp-for="@item.SubtotalItemNota" class="form-control" value="@item.SubtotalItemNota" readonly="readonly" />
                    </td>
                    <td>
                        <input asp-for="@item.QtdePesada" class="form-control" value="@item.QtdePesada" />
                    </td>
                    <td>
                        <input type="submit" value="Gravar peso" class="btn btn-info" style="margin-left:2%" />
                    </td>
                </form>
            </tr>
        }
    </tbody>
</table>


