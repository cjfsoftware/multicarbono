@model Multicarbono.Models.Pedido.Pedido

<h3>Deseja excluir o pedido?</h3>

<div>
    <form asp-action="Delete">
        <input type="hidden" asp-for="IdPedido" />
        <input type="submit" value="Sim" class="btn btn-danger" />
        <input type="button" asp-action="Index" value="Não" class="btn btn_danger" />
        <a asp-action="Index" value="Não"class="btn btn_danger"></a>
    </form>
</div>
